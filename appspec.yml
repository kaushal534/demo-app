version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html

hooks:
  AfterInstall:
    - location: demo-app/install_nginx.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: demo-app/start_nginx.sh
      timeout: 300
      runas: root
